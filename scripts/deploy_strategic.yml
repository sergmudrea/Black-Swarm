- name: Deploy Siege strategic node
  hosts: strategic
  become: yes
  vars:
    node_mode: strategic
    config_src: "../configs/node_strategic.json"
    siege_image: "sergmudrea/black-swarm:latest"
  tasks:
    - name: Install Docker
      ansible.builtin.apt:
        name:
          - docker.io
          - docker-compose
        state: present
        update_cache: yes

    - name: Ensure Docker is running
      ansible.builtin.systemd:
        name: docker
        state: started
        enabled: yes

    - name: Create config directory
      ansible.builtin.file:
        path: /etc/siege
        state: directory
        mode: '0700'

    - name: Copy node configuration
      ansible.builtin.copy:
        src: "{{ config_src }}"
        dest: /etc/siege/node_config.json
        owner: root
        group: root
        mode: '0600'

    - name: Pull Siege image
      community.docker.docker_image:
        name: "{{ siege_image }}"
        source: pull

    - name: Run Siege container
      community.docker.docker_container:
        name: siege-strategic
        image: "{{ siege_image }}"
        state: started
        restart_policy: unless-stopped
        command: ["-mode={{ node_mode }}", "-config=/etc/siege/node_config.json"]
        env:
          SIEGE_CONFIG_KEY: "{{ lookup('env', 'SIEGE_CONFIG_KEY') }}"
        volumes:
          - /etc/siege/node_config.json:/etc/siege/node_config.json:ro
          - /var/lib/siege:/data
        ports:
          - "8443:8443"
        networks:
          - name: swarm_net

    - name: Verify node is running
      ansible.builtin.uri:
        url: http://localhost:8443/api/swarm/peers
        method: GET
        status_code: 200
      register: result
      until: result.status == 200
      retries: 10
      delay: 5
